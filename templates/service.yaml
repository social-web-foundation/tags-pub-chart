apiVersion: v1
kind: Service
metadata:
  name: {{ include "tags-pub.fullname" . }}
  labels:
    {{- include "tags-pub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tags-pub.selectorLabels" . | nindent 4 }}
