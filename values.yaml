replicaCount: 1

image:
  repository: ghcr.io/social-web-foundation/tags.pub
  pullPolicy: IfNotPresent
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 9000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: tags-pub.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

env:
  DATABASE_URL: ""
  ORIGIN: ""
  PORT: "9000"
  LOG_LEVEL: "info"

extraEnv: []

# Domain blocklist: mounts a Mastodon-format denylist CSV (read via .Files from
# the chart directory) into the pod and points DOMAIN_BLOCK_LIST at it. The pod
# template carries a checksum annotation so changes to the file roll the pods.
domainBlockList:
  enabled: false
  file: domain-blocklist.csv
  mountDir: /etc/tags-pub/blocklist

startupProbe:
  enabled: true
  httpGet:
    path: /livez
    port: http
  periodSeconds: 5
  timeoutSeconds: 5
  failureThreshold: 30

livenessProbe:
  enabled: true
  httpGet:
    path: /livez
    port: http
  initialDelaySeconds: 15
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  enabled: true
  httpGet:
    path: /readyz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 6

resources:
  requests:
    cpu: 100m
    memory: 768Mi
  limits:
    memory: 1Gi

nodeSelector: {}
tolerations: []
affinity: {}
